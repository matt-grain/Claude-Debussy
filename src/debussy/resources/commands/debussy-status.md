# Check Orchestration Status

View the current Debussy orchestration status, including phase executions and progress.

## Usage

```
/debussy-status
```

## Implementation

```bash
uv run debussy status
```

If the above fails, try activating the virtual environment first:
```bash
source .venv/bin/activate && debussy status
```
